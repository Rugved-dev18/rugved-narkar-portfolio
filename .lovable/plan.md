

## Plan: Remove "Animated Portfolio" Project Card

Remove the 6th project entry ("Animated Portfolio") from the `projects` array in `src/components/ProjectsSection.tsx` (lines 51-56), along with its unused image import (`project6` on line 9).

